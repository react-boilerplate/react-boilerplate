import { createSelector } from 'reselect';
import globalSelector from 'globalSelector';

const {{ camelCase name }}Selector = createSelector(
  globalSelector
);

export default {{ camelCase name }}Selector;
