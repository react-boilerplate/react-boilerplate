import expect from 'expect';
import {{ properCase name }}Reducer from '../reducer';
import {  } from '../constants';

describe('{{ properCase name }}Reducer', () => {
  it('returns the initial state', () => {
    expect({{ properCase name }}Reducer(undefined, {})).toEqual({});
  });
});
