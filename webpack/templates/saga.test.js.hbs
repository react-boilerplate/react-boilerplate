import { take, call, put, select } from 'redux-saga/effects';
import {{camelCase name}} from '../{{camelCase name}}.saga';
import expect from 'expect';

const saga = {{camelCase name}}();

describe('{{camelCase name}} Saga', () => {

});
