// import React from 'react';
/*
import { FormattedMessage } from 'react-intl';
import { shallow } from 'enzyme';

import DisplayItems from '../index';
import messages from '../messages';

describe('<DisplayItems />', () => {
  it('should render the page header', () => {
    const renderedComponent = shallow(<DisplayItems />);
    expect(
      renderedComponent.contains(<FormattedMessage {...messages.header} />),
    ).toEqual(true);
  });
});
*/

/*
import React from 'react';
import { shallow } from 'enzyme';
// import { FormattedMessage } from 'react-intl';
import configureStore from 'redux-mock-store';
import { DisplayItems } from '../index';

const store = configureStore()();

describe('Test suite for DisplayItems container', () => {
  // const wrapper = shallow(<DisplayItems />).dive();
  // it('should exist', () => {
  //   expect(wrapper.exists()).toBeTruthy();
  // });
  const renderedComponent = shallow(<DisplayItems store={store} />).dive();
  expect(renderedComponent.find('DisplayItems').node).toBeDefined();
  // it('should render tow Wrapper Components', () => {
  //   expect(component.find('Wrapper').length).toBe(2);
  // });
  // it('should render a FormattedMessage Component', () => {
  //   expect(container.find('FormattedMessage').length).toBe(1);
  // });
  // it('should render a Response Helmet', () => {
  //   expect(container.find('Helmet').length).toBe(1);
  // });
});
*/
