/*
 * DisplayItems Messages
 *
 * This contains all the text for the DisplayItems container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DisplayItems';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DisplayItems container!',
  },
});
