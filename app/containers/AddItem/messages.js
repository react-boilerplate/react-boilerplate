/*
 * AddItem Messages
 *
 * This contains all the text for the AddItem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddItem container!',
  },
});
