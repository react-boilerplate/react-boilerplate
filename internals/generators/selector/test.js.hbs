import {{ camelCase name }}Selector from '../{{ camelCase name }}Selector';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = {{ camelCase name}}Selector();

describe('{{ camelCase name }}Selector', () => {

});
