import {
  select{{ properCase name }},
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = select{{ properCase name}}();

describe('select{{ properCase name }}', () => {

});
