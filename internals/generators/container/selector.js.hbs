import { createSelector } from 'reselect';
import globalSelector from 'globalSelector';

const {{ camelCase selectorName }}Selector = () => createSelector(
  globalSelector(),
);

export default {{ camelCase selectorName }}Selector;
