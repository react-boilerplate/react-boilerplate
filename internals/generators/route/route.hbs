 {
      path: '{{ path }}',
      getComponent: memoizeComponent((renderRoute) => {
        import('{{{directory (properCase component)}}}')
          .then(renderRoute)
          .catch(errorLoading);
      }),
    },$1