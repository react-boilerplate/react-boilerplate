import { fromJS } from 'immutable';

const initialState = fromJS({});

function {{ properCase name }}PageReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default {{ properCase name }}PageReducer;
