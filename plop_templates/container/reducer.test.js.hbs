import expect from 'expect';
import {{ properCase name }}PageReducer from '../reducer';
import {  } from '../constants';

describe('{{ properCase name }}PageReducer', () => {
  it('returns the initial state', () => {
    expect({{ properCase name }}PageReducer(undefined, {})).toEqual({});
  });
});