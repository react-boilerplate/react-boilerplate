#! /bin/bash
#
# Removes project-specific functions from the environment, if set
#
# NOTE: Only available after using "{{go}} env" to set up your shell
# environment.

@go.printf 'ERROR: "%s" is implemented in the function generated by "%s".\n' \
  "$_GO_CMD ${BASH_SOURCE[0]##*/}" "$_GO_CMD env" >&2
exit 1
